$(function () {
  "use strict";

  $(".preloader").fadeOut();
  // ==============================================================
  // Theme options
  // ==============================================================
  // ==============================================================
  // sidebar-hover
  // ==============================================================

  $(".left-sidebar").hover(
    function () {
      $(".navbar-header").addClass("expand-logo");
    },
    function () {
      $(".navbar-header").removeClass("expand-logo");
    }
  );
  // this is for close icon when navigation open in mobile view
  $(".nav-toggler").on("click", function () {
    $("#main-wrapper").toggleClass("show-sidebar");
    $(".nav-toggler i").toggleClass("ti-menu");
  });
  $(".nav-lock").on("click", function () {
    $("body").toggleClass("lock-nav");
    $(".nav-lock i").toggleClass("mdi-toggle-switch-off");
    $("body, .page-wrapper").trigger("resize");
  });
  $(".search-box a, .search-box .app-search .srh-btn").on("click", function () {
    $(".app-search").toggle(200);
    $(".app-search input").focus();
  });

  // ==============================================================
  // Right sidebar options
  // ==============================================================
  $(function () {
    $(".service-panel-toggle").on("click", function () {
      $(".customizer").toggleClass("show-service-panel");
    });
    $(".page-wrapper").on("click", function () {
      $(".customizer").removeClass("show-service-panel");
    });
  });
  // ==============================================================
  // This is for the floating labels
  // ==============================================================
  $(".floating-labels .form-control")
    .on("focus blur", function (e) {
      $(this)
        .parents(".form-group")
        .toggleClass("focused", e.type === "focus" || this.value.length > 0);
    })
    .trigger("blur");

  // ==============================================================
  //tooltip
  // ==============================================================
  $(function () {
    $('[data-toggle="tooltip"]').tooltip();
  });
  // ==============================================================
  //Popover
  // ==============================================================
  $(function () {
    $('[data-toggle="popover"]').popover();
  });

  // ==============================================================
  // Perfact scrollbar
  // ==============================================================
  $(".message-center, .customizer-body, .scrollable").perfectScrollbar({
    wheelPropagation: !0,
  });

  /*var ps = new PerfectScrollbar('.message-body');
    var ps = new PerfectScrollbar('.notifications');
    var ps = new PerfectScrollbar('.scroll-sidebar');
    var ps = new PerfectScrollbar('.customizer-body');*/

  // ==============================================================
  // Resize all elements
  // ==============================================================
  $("body, .page-wrapper").trigger("resize");
  $(".page-wrapper").show();
  // ==============================================================
  // To do list
  // ==============================================================
  $(".list-task li label").click(function () {
    $(this).toggleClass("task-done");
  });

  //****************************
  /* This is for the mini-sidebar if width is less then 1170*/
  //****************************
  var setsidebartype = function () {
    var width = window.innerWidth > 0 ? window.innerWidth : this.screen.width;
    if (width < 1170) {
      $("#main-wrapper").attr("data-sidebartype", "mini-sidebar");
    } else {
      $("#main-wrapper").attr("data-sidebartype", "full");
    }
  };
  $(window).ready(setsidebartype);
  $(window).on("resize", setsidebartype);
  //****************************
  /* This is for sidebartoggler */
  //****************************
  $(".sidebartoggler").on("click", function () {
    const mainWrapper = $("#main-wrapper");
	// Get the icon inside the sidebar toggle button
    const icon = $(".sidebartoggler i");  

    mainWrapper.toggleClass("mini-sidebar");

    if (mainWrapper.hasClass("mini-sidebar")) {
        // If the sidebar is collapsed (mini), rotate the icon 180° to simulate the right arrow
        icon.css("transform", "rotate(180deg)");
        mainWrapper.attr("data-sidebartype", "mini-sidebar");
    } else {
        // If the sidebar is expanded (full), reset the icon to its original state
        icon.css("transform", "rotate(0deg)");
        mainWrapper.attr("data-sidebartype", "full");
    }
  });

  //******************************************
  /* Uncheck checkbox if input field is empty */
  //******************************************
  
  //function to check if input field is empty or not
  function checkFieldsAndUncheck(checkboxId, inputIds) {
	//return true if all input field is empty; checkbox is unchecked
	let allEmpty = inputIds.every(id => document.getElementById(id).value === "");
	
	//return false if even one of the input field is filled; checkbox stay checked
	document.getElementById(checkboxId).checked = !allEmpty;
  }
  
  /* event listener to detect changes in the input field; then call the function */
  //electrical
  let elecInput = ["elecdirec1", "elecdirec2", "eleceng1", "eleceng2"];
  elecInput.forEach(id => {
	  document.getElementById(id).addEventListener("input", function() {
		  checkFieldsAndUncheck("elec", elecInput);
	  });
  });
  //mechanical
  let mechInput = ["mechdirec1", "mechdirec2", "mecheng1", "mecheng2"];
  mechInput.forEach(id => {
	  document.getElementById(id).addEventListener("input", function() {
		  checkFieldsAndUncheck("mech", mechInput);
	  });
  });
  //other
  let otherInput = ["otherdirec1", "otherdirec2", "othereng1", "othereng2"];
  otherInput.forEach(id => {
	  document.getElementById(id).addEventListener("input", function() {
		  checkFieldsAndUncheck("other", otherInput);
	  });
  });
  
  //TO BE DLETED LATER IF NOT USED!!
  /* Category Tabs 
  function openContent(evt, category) {
	  // Declare all variables
	  var i, tabcontent, tablinks;1

	  // Get all elements with class="tabcontent" and hide them
	  tabcontent = document.getElementsByClassName("tabcontent");
	  for (i = 0; i < tabcontent.length; i++) {
		tabcontent[i].style.display = "none";
	  }

	  // Get all elements with class="tablinks" and remove the class "active"
	  tablinks = document.getElementsByClassName("tablinks");
	  for (i = 0; i < tablinks.length; i++) {
		tablinks[i].className = tablinks[i].className.replace(" active", "");
	  }

	  // Show the current tab, and add an "active" class to the button that opened the tab
	  document.getElementById(cityName).style.display = "block";
	  evt.currentTarget.className += " active";
  }*/
  
});